using Godot;
using System;

public partial class Ui : Control
{
    public override void _Ready() {
        // Settings.Instance.chargeLabel = GetNode<Label>("charge label");
        // Settings.Instance.enemyHealthBar = GetNode<ProgressBar>("health bar");
        // Settings.Instance.fpsLabel = GetNode<Label>("fps");
        // Settings.Instance.speedLabel = GetNode<Label>("SpeedLabel");
        // Settings.Instance.TimeSlowBar = GetNode<ProgressBar>("TimeSlowBar");
    }
}
